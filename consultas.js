// Mostrar albumes  del genero jazz
db.album.find(genre{sin:["jazz"]})
// mostrar albumes con precio mayor a 50000
db.album.find(price{sin:[50000]})
// mostrar albumes que no sean rock
db.album.find(genre{sin:["jazz"]})
// mostrar sku,title y tags con calificaion mayor a 4.5
db.album.find(genre{sin:["jazz"]})
// mostrar sku title y price de los albumes con stock menor a 5
db.album.find(genre{sin:["jazz"]})